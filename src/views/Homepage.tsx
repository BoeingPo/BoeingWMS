import React from "react";

const HomePage = () => {

    return(
        <div style={{display:'flex',justifyContent:'flex-start',textAlign:'start'}}> This is HomePage View Taw is in the respository
        <div>ทดสอบการเชื่อม git</div>
        </div>
    );
}

export default HomePage;